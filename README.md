# SiN-Serve-FileTransferEngine
The file transfer engine for SiN-Serve, a file transfer server for mIRC written in Python and using UDP high speed transfers when both parties use SiN-Serve, defaulting to basic DCC send when the receiver doesn't support it.
